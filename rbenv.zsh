# If you use rbenv with the default configuration, this will shim your path correctly
# Otherwise, configure this file like you would have your .bash_profile when you
#    installed rbenv originally

export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"
